---

- name: Apply the Rocket.Chat role to all chat_servers
  hosts: chat_servers

  roles:
    # Clone as RochetChat.Server from git or test from ansible-galaxy install
    - ../../RocketChat.Server
